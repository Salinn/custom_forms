<%= form_for(@answer_type) do |f| %>
  <% if @answer_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@answer_type.errors.count, "error") %> prohibited this answer_type from being saved:</h2>

      <ul>
      <% @answer_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :type %><br>
    <%= f.text_field :type %>
  </div>
  <div class="field">
    <%= f.label :quantity %><br>
    <%= f.number_field :quantity %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
